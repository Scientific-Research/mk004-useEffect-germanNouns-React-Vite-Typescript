- x fetch nouns and display on page
- x enable user to click and show answer of flashcard
  - x add additional property to each object: isOpen
   - x onClick
   - x handleToggleFlashCard()
- x create button "Mark as learned"
  - x add additional property to each object: isLearned
  - x onClick
  - x handleMarkAsLearnedClick
  - x if isLearned, then don't show the button: {!isLearned && ()}
  - .. show "You have learned nnn of 124 nouns."

-  localStorage so that when user comes back to page, their progress is restored!